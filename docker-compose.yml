version: '3.8'

services:
  rag-system:
    build: .
    volumes:
      - ./data:/app/data
      - ./samples:/app/samples
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: interactive
